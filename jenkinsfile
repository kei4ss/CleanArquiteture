pipeline {
    agent any
    stages {
        stage('Build docker image') {
            steps {
                script {
                    sh 'docker compose up --build'
                }
            }
        }
    }
}
