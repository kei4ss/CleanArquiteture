pipeline {
    agent any
    stages {
        stage('Build docker image') {
            steps {
                script {
                    sh 'TESTE DE VERSAO DO DOCKER'
                    sh 'docker --version'
                    sh 'docker build -t my-image:latest .'
                }
            }
        }
    }
}
