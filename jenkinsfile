pipeline {
    agent any
    stages {

        stage('Building project') {
            steps {
                script {
                    sh 'docker --version'
                    sh 'docker build -t my-image:latest .'
                }
            }
        }
    }
}
