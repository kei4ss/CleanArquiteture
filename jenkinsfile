pipeline {
    agent any
    stages {

        stage('Testing docker version') {
            steps {
                script {
                    sh 'docker --version'
                }
            }
        }

        stage('Build docker image') {
            steps {
                script {
                    sh 'docker build -t my-image:latest Dockerfile'
                }
            }
        }
    }
}
